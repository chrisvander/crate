apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ssl

patchesJson6902:
  - target:
      group: "cert-manager.io"
      version: v1
      kind: ClusterIssuer
      name: letsencrypt-prod
    path: cert-patch.json

resources:
  - certs.yml
