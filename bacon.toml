default_job = "server"

[jobs.server]
command = ["cargo", "run", "-p", "crate-server"]
need_stdout = true
background = false
on_change_strategy = "kill_then_restart"
watch = ["packages/protocol/src", "packages/server/src"]
